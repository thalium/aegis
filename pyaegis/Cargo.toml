[package]
name = "pyaegis"
version = "0.1.0"
edition = "2021"

[lib]
name = "pyaegis"
crate-type = ["cdylib"]

[dependencies]
pyo3 = "0.27.2"
libaegis = { path = "../libaegis", features = ["std"] }
client = { path = "../client" }
rand = "0.9.2"
